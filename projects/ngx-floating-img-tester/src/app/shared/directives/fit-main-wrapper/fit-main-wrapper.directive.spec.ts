import { FitMainWrapperDirective } from './fit-main-wrapper.directive';

describe('FitMainWrapperDirective', () => {
  it('should create an instance', () => {
    //const directive = new FitMainWrapperDirective();
    //expect(directive).toBeTruthy();
  });
});
